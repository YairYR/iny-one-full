/* INACTIVO — barrel vacío del módulo `users` (rev. 2026-08-09).
 * No exporta nada y ningún fichero lo importa. Se conserva como punto de
 * entrada previsto para el módulo; al usarlo, reexportar aquí su API pública. */
export {};
